# 🦙 Test-Hae-Llama Project Master Plan

## 🎯 Core Objective
로컬 7b 모델(Qwen 등)을 사용하여 복잡한 엔터프라이즈급 자바 프로젝트의 유닛 테스트를 완벽하게 생성하고, 스프링 표준 경로에 자동 저장하는 "저사양 친화적" AI 엔진을 구축한다.

## 🛠️ Key Technical Requirements

### 1. ⚡ 저사양 자원 최적화 (Resource-First)
- **Strict Sequential Processing:** 모든 LLM 호출은 차례대로 수행하여 CPU/RAM 부하를 최소화한다.
- **Context Slicing:** 7b 모델의 VRAM 부담을 줄이기 위해 전체 소스 코드 대신 필드와 메서드 시그니처만 포함된 초경량 문맥을 제공한다.
- **Graceful Recovery:** 리소스 부족으로 개별 태스크 실패 시, 해당 조각만 주석 처리하고 프로세스를 유지한다.

### 2. 🧩 원자적 분할 정복 (Atomic Divide & Conquer)
- 작업을 클래스 단위가 아닌 **개별 테스트 시나리오 단위**로 쪼개어 생성한다.
- Architect는 성공 케이스(1개)보다 **실패/엣지 케이스(무제한)** 발굴에 집중한다.
- 각 시나리오는 독립적인 태스크로 큐잉되어 순차적으로 정복된다.

### 3. 🧪 공격적 버그 사냥 (Bug Hunter Mindset)
- **Comprehensive Negative Testing:** Null, Empty, Boundary, Exception 흐름을 집요하게 파고든다.
- **Parameterized Testing:** `@ParameterizedTest`와 `@CsvSource`를 활용해 다양한 실패 데이터를 우아하게 처리한다.

### 4. 💎 클린 코드 및 표준 준수
- **Java 15+ Text Blocks:** JSON 및 멀티라인 문자열에 `"""`를 강제하여 이스케이프 실수를 방지한다.
- **Spring Path Intelligence:** `src/test/java` 하위의 정확한 패키지 구조에 자동 저장하며 멀티 모듈을 지원한다.
- **Mandatory Disclaimer:** 모든 클래스 상단에 "🚨 이 테스트 코드는 반드시 통과되어야 합니다라마!" 주석을 명시한다.

## 🚨 Current Status
- v0.2.2 기준: 저사양 최적화 및 원자적 생성 로직 안착 완료.
- 다음 과제: 실제 복잡한 비즈니스 로직에서의 코드 생성 정확도(Hallucination 차단) 지속 모니터링.

## 💬 Decision History
- [2025-12-26] 병렬 처리 금지 및 순차 처리 강제 (하드웨어 보호).
- [2025-12-26] 텍스트 블록 문법 도입으로 7b 모델의 고질적 이스케이프 에러 해결.
- [2025-12-26] 시나리오 개수 제한 해제 및 "실패 위주" 설계 원칙 확립.
