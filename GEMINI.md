# 🦙 Test-Hae-Llama CLI: 개발자 가이드 (v2.0)

이 문서는 본 프로젝트의 핵심 원칙과 개발 가이드라인을 정의합니다. AI 에이전트(Antigravity)는 이 규칙을 절대적으로 준수해야 합니다.

---

## 🚨 1. 절대 규칙 (ABSOLUTE RULES)

### **Rule #1: 수동 개입 금지 (Deep Engineering Protocol)**
- **규칙:** Antigravity는 `opencode_response.txt` 파일을 수동으로 작성, 수정, 생성해서는 안 된다. 이 파일은 CLI에 의해 내부적으로 생성 및 관리되는 파일이다.
- **이유:** 우리는 "운 좋게 통과하는 테스트"가 아니라, **"어떤 상황에서도 통과하는 강력한 엔진"**을 만드는 것이 목표이다.
- **행동 강령:**
    - 생성된 테스트 코드가 올바르지 않다면, 결과물을 고치지 말고 **Java 엔진 로직(Prompt Engineering, Context Analyzer)**을 수정하라.
    - LLM이 이해할 수 있도록 `Intelligence` 추출 로직이나 `Context` 구성 방식을 개선하여, LLM이 스스로 정답을 맞추도록 유도해야 한다.

### **Rule #2: 엔진 우선 검증 (Engine-First Testing)**
- 통합 테스트(`generate` 명령 실행)보다 **단위 테스트(:common:test)**를 우선시한다.
- 엔진의 변경 사항(예: Prompt 변경, Context 추가)은 반드시 `common` 모듈의 단위 테스트로 검증되어야 한다.

---

## 🏗️ 2. 아키텍처 및 워크플로우

### **핵심 철학**
- **Method-Level Iteration:** 소스 코드를 메서드 단위로 분해하여 정밀하게 테스트를 생성한다.
- **AST Analysis:** 단순 텍스트 처리가 아닌, JavaParser를 사용한 AST(추상 구문 트리) 분석을 기반으로 한다.

### **파이프라인 단계**
1.  **Analysis (분석):** 소스 코드 파싱, 의존성 식별, 메타데이터(`Intelligence`) 추출.
2.  **Strategy (전략):** 테스트 시나리오(성공, 실패, 예외) 수립.
3.  **Coding (구현):** 
    - **Global Setup:** `@BeforeEach`, Mocking 등 클래스 기본 구조 생성.
    - **Method Test:** 개별 메서드에 대한 테스트 케이스 생성 (반복).
4.  **Synthesis (합성):** 생성된 조각들을 하나의 완벽한 Java 클래스로 조립 및 정제.

---

## 🛡️ 3. Llama Security Protocol (LSP)
- **Masking:** LLM으로 전송되는 모든 소스 코드는 보안상 민감한 정보가 제거(Masking)되어야 한다.
- **Sanitization:** LLM이 생성한 응답에서 XML/Markdown 태그를 제거하고 순수 Java 코드만 추출한다.

---

## 🛠️ 4. 기술 스택 및 환경
- **Language:** Java 17+
- **Build Tool:** Gradle
- **Parsing:** JavaParser
- **Test:** JUnit 5, Mockito, AssertJ

> **Note:** "Collaboration is our Engine. Precision is our Law."
