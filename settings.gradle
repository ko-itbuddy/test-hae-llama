pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal() // Generally good practice to include this for plugins
    }
}

rootProject.name = 'local-test-code-llm'

include 'common'
include 'intellij-plugin'